{
  "name": "claimless-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Envio HyperIndex indexer for Claimless (IncidentRegistry + RiskScore) on Monad testnet (chain 10143)",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "codegen": "envio codegen",
    "dev": "envio dev",
    "start": "envio start",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  },
  "dependencies": {
    "envio": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}