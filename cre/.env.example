# CRE workflow environment (copy to .env; never commit .env)
#
# The workflow needs no secrets today: it only reads public contract state.
# When writeReport is wired (Week 3) this key will sign the on-chain write.
CRE_ETH_PRIVATE_KEY=
# KeystoneForwarder address on Monad testnet, discovered after deploy approval.
CRE_FORWARDER_ADDRESS=
